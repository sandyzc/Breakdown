<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sandyzfeaklab.breakdown_app">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Breakdown">
        <activity
            android:name=".Excel2Firestore"
            android:exported="true" />
        <activity
            android:name=".Report"
            android:exported="true" />
        <activity android:name=".EditPending_Activity" />
        <activity android:name=".Pending_activity" />
        <activity
            android:name=".Add_Log"
            android:label="@string/title_activity_add__log"
            android:theme="@style/Theme.Breakdown" />
        <activity android:name=".Add_Sap_codes" />
        <activity android:name=".Mttr" />
        <activity
            android:name=".Data_input"
            android:label="@string/title_activity_data_input"
            android:theme="@style/Theme.Breakdown.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Breakdown.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>